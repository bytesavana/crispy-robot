/**
 * Zone selection has no UI yet — isolating this here means wiring up real zone selection
 * later only touches this file. Customer id is no longer hardcoded: see deviceId.ts (guest
 * fallback) and auth.ts (signed-in token).
 */
export const HARDCODED_ZONE_NAME = "Lifestyle Heights, Tatu City";
